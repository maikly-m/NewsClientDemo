<resources >

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar" >
        <!-- Customize your theme here. -->
        <item name="colorPrimary" >@color/colorPrimary</item >
        <item name="colorPrimaryDark" >@color/colorPrimaryDark</item >
        <item name="colorAccent" >@color/colorAccent</item >
        <item name="android:windowNoTitle">true</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
        <item name="actionBarSize">40dp</item>
        <!--引用CustomTabPageIndicator-->
        <item name="vpiTabPageIndicatorStyle">@style/CustomTabPageIndicator</item>

    </style >
    
    <style name="menuStyle">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">#77ff0000</item>
    </style>
    <!--TabPageIndicator样式-->
    <style name="CustomTabPageIndicator" parent="Widget.TabPageIndicator">
        <item name="android:background">@drawable/tab_indicator</item>
        <item name="android:textAppearance">@style/CustomTabPageIndicator.Text</item>
        <item name="android:textSize">14sp</item>
        <item name="android:dividerPadding">8dp</item>
        <item name="android:showDividers">middle</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
        <item name="android:paddingTop">5dp</item>
        <item name="android:paddingBottom">5dp</item>
        <item name="android:fadingEdge">horizontal</item>
        <item name="android:fadingEdgeLength">8dp</item>
    </style>
    <style name="CustomTabPageIndicator.Text">
        <item name="android:textStyle">normal</item>
        <item name="android:textColor">@drawable/selector_tabtext</item>
    </style>
     <!--水平progressbar样式-->
    <style name="CustomProgressBarHorizotal" parent="@android:style/Widget.ProgressBar.Horizontal" >
        <item name="android:progressDrawable">@drawable/custom_progressbar_horizotal</item>
        <item name="android:minHeight">3dp</item>
        <item name="android:maxHeight">3dp</item>

    </style>
    <!--popu动画-->
    <style name="CustomPopuAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/popu_animation_show</item>
        <item name="android:windowExitAnimation">@anim/popu_animation_hide</item>
    </style>
    
</resources >
